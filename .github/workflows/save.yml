name: 'Save Dist'
run-name: ${{github.actor}} is Saving

on:
  push:
    branches:
      - master
      - main
      - dev
env:
  environment: TESSSt 👽👽👽

jobs:
  RunBuild:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - uses: actions/checkout@v4
      - name: Running ${{env.environment}} $environment
        run: echo $environment ${{env.environment}}
      - name: Use Node.js ${{matrix.node-version}}
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
      - run: npm ci
      - run: npm run build --if-present
      - name: Archive Artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-without-markdown
          path: |
            dist
            !dist/**/*.md
          overwrite: true
